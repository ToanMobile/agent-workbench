---
description: "Deprecated alias: /test was renamed to /check (skill qc); removed in DevKit 1.2.0."
---
<!-- devkit:deprecated-alias -->
# /test → /check

`/test` is a deprecated DevKit alias. Use `/check` (skill `qc`) from now on.

Run the `qc` skill now: read `skills/qc/SKILL.md` (in an installed
project: `.agents/skills/qc/SKILL.md`) and follow it for: $ARGUMENTS

Tell the user once, in one line, that `/test` is deprecated and `/check` replaces it.
