---
description: "Deprecated alias: /crashlytics was renamed to /fix (skill fixbugs); removed in DevKit 1.2.0."
---
<!-- devkit:deprecated-alias -->
# /crashlytics → /fix

`/crashlytics` is a deprecated DevKit alias. Use `/fix` (skill `fixbugs`) from now on.

Run the `fixbugs` skill now: read `skills/fixbugs/SKILL.md` (in an installed
project: `.agents/skills/fixbugs/SKILL.md`) and follow it for: $ARGUMENTS

Tell the user once, in one line, that `/crashlytics` is deprecated and `/fix` replaces it.
