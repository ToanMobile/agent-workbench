---
description: "Deprecated alias: /bugs was renamed to /fix (skill fixbugs); removed in DevKit 1.2.0."
---
<!-- devkit:deprecated-alias -->
# /bugs → /fix

`/bugs` is a deprecated DevKit alias. Use `/fix` (skill `fixbugs`) from now on.

Run the `fixbugs` skill now: read `skills/fixbugs/SKILL.md` (in an installed
project: `.agents/skills/fixbugs/SKILL.md`) and follow it for: $ARGUMENTS

Tell the user once, in one line, that `/bugs` is deprecated and `/fix` replaces it.
