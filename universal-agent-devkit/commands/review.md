---
description: "Deprecated alias: /review was renamed to /plan-tests (skill qa-review); removed in DevKit 1.2.0."
---
<!-- devkit:deprecated-alias -->
# /review → /plan-tests

`/review` is a deprecated DevKit alias. Use `/plan-tests` (skill `qa-review`) from now on.

Run the `qa-review` skill now: read `skills/qa-review/SKILL.md` (in an installed
project: `.agents/skills/qa-review/SKILL.md`) and follow it for: $ARGUMENTS

Tell the user once, in one line, that `/review` is deprecated and `/plan-tests` replaces it.
